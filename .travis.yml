language: python
python:
  - "3.8.0"
install:
  - pip install -r requirements.txt
script:
  - python manage.py
deploy:
  provider: pages
  cleanup: true
  api_token: github_token  # Set in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: main